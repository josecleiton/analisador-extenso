#ifndef INCLUSOS
    #define INCLUSOS
    #include "preproc.h"
#endif

char* cardinalNumeral (char* card);

char* cardinalNumeral (char* card)
{
    int cursor = 0, i = 0, k;
    char* flag = NULL;
    char* num = (char*) malloc (strlen(card));
    return num;
}
