#include "extenso/state.h"

/*
**  Definições das variáveis globais (TEMPORÁRIO — ver state.h).
*/
Ordem ref;
char *EXP;
char *_TEXP;
char expNum[MAX_GEN];
char token;
short tipoToken;
unsigned flagNUM;
uint16_t *ind;
FILE *dicionario;
ListaNum *list;
