#include "extenso/state.h"

/*
**  Definições das variáveis globais (TEMPORÁRIO — ver state.h).
*/
char *EXP;
char *_TEXP;
char expNum[MAX_GEN];
char token;
short tipoToken;
unsigned flagNUM;
ListaNum *list;

const Dictionary *dict;
const ErrorTable *errtab;
